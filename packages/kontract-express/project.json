{
  "name": "@kontract/express",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/kontract-express/src",
  "projectType": "library",
  "implicitDependencies": ["kontract", "@kontract/ajv"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["kontract:build", "@kontract/ajv:build"],
      "inputs": [
        "{projectRoot}/src/**/*",
        "{projectRoot}/package.json",
        "{projectRoot}/tsconfig.json"
      ],
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "npm run build",
        "cwd": "packages/kontract-express"
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --watch",
        "cwd": "packages/kontract-express"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "dependsOn": ["kontract:build", "@kontract/ajv:build"],
      "inputs": [
        "{projectRoot}/src/**/*",
        "{projectRoot}/tests/**/*",
        "{projectRoot}/package.json",
        "{projectRoot}/tsconfig.json",
        "{projectRoot}/tsconfig.test.json"
      ],
      "options": {
        "command": "npm run test",
        "cwd": "packages/kontract-express"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint src/",
        "cwd": "packages/kontract-express"
      }
    },
    "lint:fix": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint src/ --fix",
        "cwd": "packages/kontract-express"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "dependsOn": ["kontract:build", "@kontract/ajv:build"],
      "inputs": [
        "{projectRoot}/src/**/*",
        "{projectRoot}/package.json",
        "{projectRoot}/tsconfig.json"
      ],
      "options": {
        "command": "tsc --noEmit",
        "cwd": "packages/kontract-express"
      }
    }
  },
  "tags": ["type:library", "framework:express", "scope:shared"]
}
