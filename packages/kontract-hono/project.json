{
  "name": "@kontract/hono",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/kontract-hono/src",
  "projectType": "library",
  "implicitDependencies": ["kontract", "@kontract/ajv"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["kontract:build", "@kontract/ajv:build"],
      "inputs": [
        "{projectRoot}/src/**/*",
        "{projectRoot}/package.json",
        "{projectRoot}/tsconfig.json"
      ],
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "npm run build",
        "cwd": "packages/kontract-hono"
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --watch",
        "cwd": "packages/kontract-hono"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "dependsOn": ["kontract:build", "@kontract/ajv:build"],
      "inputs": [
        "{projectRoot}/src/**/*",
        "{projectRoot}/tests/**/*",
        "{projectRoot}/package.json",
        "{projectRoot}/tsconfig.json",
        "{projectRoot}/tsconfig.test.json"
      ],
      "options": {
        "command": "npm run test",
        "cwd": "packages/kontract-hono"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint src/",
        "cwd": "packages/kontract-hono"
      }
    },
    "lint:fix": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint src/ --fix",
        "cwd": "packages/kontract-hono"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "dependsOn": ["kontract:build", "@kontract/ajv:build"],
      "inputs": [
        "{projectRoot}/src/**/*",
        "{projectRoot}/package.json",
        "{projectRoot}/tsconfig.json"
      ],
      "options": {
        "command": "tsc --noEmit",
        "cwd": "packages/kontract-hono"
      }
    }
  },
  "tags": ["type:library", "framework:hono", "scope:shared"]
}
